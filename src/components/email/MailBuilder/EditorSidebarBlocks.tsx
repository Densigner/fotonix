// Re-export the JSX implementation so imports can reference the TS filename if needed.
// Minimal TypeScript stub to satisfy imports in environments that reference the .tsx path.
// This file intentionally avoids importing React typings or JSX to prevent typecheck errors in this repo.
// @ts-nocheck

const EditorSidebarBlocks: any = () => null;
export default EditorSidebarBlocks;
