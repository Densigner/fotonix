# Copy this file to .env and replace the value with your secret key
# IMPORTANT: do NOT commit your real key to source control. Put your real key into a local
# file named .env (which is already ignored by .gitignore).
OPENAI_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
